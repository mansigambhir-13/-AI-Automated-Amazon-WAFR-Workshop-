---
phase: 05-data-migration-and-audit-validation
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md
autonomous: false
requirements:
  - OPER-01

must_haves:
  truths:
    - "Team user can log in, create assessment, run analysis, approve review decisions, download report, and log out"
    - "Client user can log in and see only their own assessments — other session IDs are not visible"
    - "Audit log entries exist in wafr-audit-log DynamoDB table after smoke test actions"
    - "Migrated sessions are visible in the WafrTeam user's dashboard after login"
    - "Re-running migration creates no duplicate entries (idempotency confirmed)"
  artifacts:
    - path: ".planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md"
      provides: "Step-by-step manual smoke test checklist for both user roles"
      contains: "WafrTeam"
  key_links:
    - from: "Frontend login (Amplify)"
      to: "Backend JWT middleware"
      via: "Authorization: Bearer header"
      pattern: "Bearer"
    - from: "Backend endpoints"
      to: "wafr-audit-log DynamoDB table"
      via: "AuditMiddleware writes on every request"
      pattern: "audit"
---

<objective>
Create a manual smoke test checklist and execute end-to-end validation of the full WAFR platform: verify migrated data is accessible, test both WafrTeam and WafrClients user roles, confirm audit log entries, and validate migration idempotency.

Purpose: This is the final milestone validation — confirming that all five phases (infrastructure, storage, auth, frontend, migration) work together as a complete system. The manual checklist format was locked in the user discussion. No new code is written; this plan validates everything built in Phases 1-4 and deployed in Plan 05-01.

Output: Smoke test checklist document and human-verified pass/fail results.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-migration-and-audit-validation/05-CONTEXT.md
@.planning/phases/05-data-migration-and-audit-validation/05-RESEARCH.md
@.planning/phases/05-data-migration-and-audit-validation/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke test checklist and validate migration idempotency</name>
  <files>.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md</files>
  <action>
Create a step-by-step manual smoke test checklist at `.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md`. The checklist must cover all locked decisions from CONTEXT.md.

**Structure the checklist with these sections:**

**Section 1: Pre-Flight Checks**
- [ ] Both App Runner services show status `RUNNING`
- [ ] Unauthenticated `curl -s -o /dev/null -w "%{http_code}" https://i5kj2nnkxd.us-east-1.awsapprunner.com/sessions` returns `401`
- [ ] `aws dynamodb scan --table-name wafr-sessions --select COUNT --region us-east-1` shows Count >= 10
- [ ] `aws dynamodb scan --table-name wafr-review-sessions --select COUNT --region us-east-1` shows Count > 0

**Section 2: Migration Idempotency Verification**
Record the DynamoDB item counts from Section 1. Then trigger a backend redeployment (or restart) so the entrypoint runs migration again:
```bash
aws apprunner start-deployment \
  --service-arn arn:aws:apprunner:us-east-1:842387632939:service/wafr-backend/aa3b1b32d7944f65b5aa1eb76c89357f \
  --region us-east-1
```
Wait for RUNNING status, then re-check counts:
- [ ] wafr-sessions count is identical to pre-restart count (no duplicates)
- [ ] wafr-review-sessions count is identical to pre-restart count (no duplicates)

**Section 3: WafrTeam User Smoke Test**
Login credentials: username `wafr-team-test` (password provided separately by operator)

1. [ ] Navigate to `https://3fhp6mfj7u.us-east-1.awsapprunner.com` — login page appears
2. [ ] Enter team user credentials — login succeeds, dashboard loads
3. [ ] User badge shows username and "WafrTeam" role indicator in header
4. [ ] Dashboard shows list of existing assessments (migrated sessions visible)
5. [ ] "New Assessment" button is visible (team users can create assessments)
6. [ ] Click "New Assessment" — assessment creation form loads
7. [ ] Fill in assessment details and start a new assessment run
8. [ ] Assessment analysis runs to completion (SSE streaming works with auth)
9. [ ] Review decisions page loads — approve at least one item
10. [ ] Download PDF report — file downloads successfully (blob download with auth header works)
11. [ ] Click Sign Out — returns to login page
12. [ ] Attempt to navigate directly to dashboard URL — redirected back to login (session cleared)

**Section 4: WafrClients User Smoke Test**
Login credentials: username `wafr-client-test` (password provided separately by operator)

1. [ ] Navigate to `https://3fhp6mfj7u.us-east-1.awsapprunner.com` — login page appears
2. [ ] Enter client user credentials — login succeeds, dashboard loads
3. [ ] User badge shows username and "WafrClients" role indicator in header
4. [ ] "New Assessment" button is NOT visible (client users cannot create assessments)
5. [ ] Dashboard shows ONLY sessions associated with this client (empty if none assigned)
6. [ ] If sessions visible, click one — session detail loads correctly
7. [ ] Attempt to access a known team session ID directly via URL — returns 403 or empty/redirect (not the session data)
8. [ ] Click Sign Out — returns to login page

**Section 5: Audit Log Verification**
After completing Sections 3 and 4:
```bash
# Count audit entries (should be > 0 after smoke test)
aws dynamodb scan --table-name wafr-audit-log --select COUNT --region us-east-1

# View recent audit entries (check for user_id, action_type, path, status_code fields)
aws dynamodb scan --table-name wafr-audit-log --region us-east-1 --output table
```
- [ ] Audit log count is greater than 0
- [ ] At least one entry shows the team user's requests (path contains `/api/wafr/` or `/sessions`)
- [ ] Entries contain `user_id`, `action_type` (or equivalent), `path`, `status_code`, and `timestamp_session_id` fields

**Section 6: Spot-Check Migrated Data**
```bash
# Verify a specific known session exists (use any session_id from the original file-based sessions)
aws dynamodb get-item \
  --table-name wafr-sessions \
  --key '{"session_id":{"S":"1225d11d-c2ce-4236-b7fc-41d54b31a5e5"}}' \
  --region us-east-1
```
- [ ] Item is returned with session data (not empty/null)
- [ ] Session data includes `session_id`, `created_at`, `status`, and `items` fields

**Section 7: Pass/Fail Summary**
| Check | Result |
|-------|--------|
| Auth enforced (401 on unauthenticated) | PASS / FAIL |
| Migration data present in DynamoDB | PASS / FAIL |
| Migration idempotency (no duplicates on re-run) | PASS / FAIL |
| WafrTeam full lifecycle | PASS / FAIL |
| WafrClients role isolation | PASS / FAIL |
| Audit log entries present | PASS / FAIL |
| **Overall** | **PASS / FAIL** |

Pass criteria per CONTEXT.md: "any auth or data issue blocks the milestone"
  </action>
  <verify>
    <automated>test -f /mnt/c/Users/LENOVO/Desktop/wafrwafrwafr/.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md && grep -q "WafrTeam" /mnt/c/Users/LENOVO/Desktop/wafrwafrwafr/.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md && grep -q "WafrClients" /mnt/c/Users/LENOVO/Desktop/wafrwafrwafr/.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md && grep -q "wafr-audit-log" /mnt/c/Users/LENOVO/Desktop/wafrwafrwafr/.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md && grep -q "idempotency" /mnt/c/Users/LENOVO/Desktop/wafrwafrwafr/.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md && echo "PASS"</automated>
    <manual>Review the checklist for completeness — all 7 sections present with specific steps</manual>
  </verify>
  <done>Smoke test checklist document exists with sections for pre-flight, idempotency, team user lifecycle, client role isolation, audit log, spot-check, and pass/fail summary</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute smoke test and confirm milestone completion</name>
  <files>.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md</files>
  <action>Operator walks through the smoke test checklist in the browser and AWS CLI. Manual verification per locked decision — no automated scripts. Covers both user roles, audit log, and migration idempotency.</action>
  <verify>Section 7 Pass/Fail summary in the checklist shows all checks PASS</verify>
  <done>All smoke test sections pass; milestone is complete with durable storage, auth enforcement, role isolation, and audit trail verified end-to-end</done>
  <what-built>
The smoke test checklist has been created at `.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md`.

The operator walks through every section in the checklist using the browser (frontend) and AWS CLI (audit log / DynamoDB verification). This is a manual verification — no automated scripts per the locked decision.

All infrastructure, storage, authentication, frontend, and migration code from Phases 1-4 is deployed and running. This checkpoint validates the complete system works end-to-end.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/05-data-migration-and-audit-validation/05-SMOKE-TEST-CHECKLIST.md`
2. Execute each section in order (Sections 1-6)
3. Fill in the Pass/Fail summary in Section 7
4. Report overall result

**Critical pass/fail criteria:**
- AUTH_REQUIRED is active (401 on unauthenticated)
- Migration data exists in DynamoDB (>= 10 sessions)
- Re-running migration produces no duplicates
- Team user completes full lifecycle (login -> assess -> review -> download -> logout)
- Client user sees only their own sessions
- Audit log has entries after smoke test

**If ANY check fails:** Describe the failure. Do NOT mark as passed. Emergency rollback commands are in Plan 05-01.
  </how-to-verify>
  <resume-signal>Type "milestone-complete" if all checks pass, or describe specific failures.</resume-signal>
</task>

</tasks>

<verification>
- [ ] Smoke test checklist document created with all 7 sections
- [ ] Operator has walked through each section and recorded results
- [ ] All checks pass — no auth or data issues blocking the milestone
- [ ] Audit log entries confirmed in wafr-audit-log
- [ ] Migration idempotency confirmed (no duplicates on re-run)
</verification>

<success_criteria>
- Smoke test checklist exists and covers both user roles, audit log, migration idempotency
- All 6 checklist sections pass (Section 7 overall = PASS)
- Milestone is complete: durable storage, auth enforcement, role isolation, audit trail all working end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-migration-and-audit-validation/05-02-SUMMARY.md`
</output>
